let selector = document.getElementById("sort-selector");
let searchForm = document.getElementById("search-form");

selector.addEventListener("change", sortHandler);
searchForm.addEventListener("submit", submitHandler);
